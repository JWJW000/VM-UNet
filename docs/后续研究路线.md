# VM-UNet 全监督研究路线

更新：2026-09-07。当前目标：使用全部训练标注，在公平协议下提高 VM-UNet 分割表现。先定位错误，再选择方法；不承诺提升幅度、创新性或录用概率。

旧扫描半监督路线已暂停，保留在 [历史文档](archive/扫描半监督路线_已暂停.md)。旧脚本和实验结果继续保留。半监督实验不再是当前主线，不自动启动、停止或排队任何远程任务。

## 1. 已有证据与限制

服务器旧实验的全监督最佳模型：

```text
results/vmunet_isic18_Friday_04_September_2026_11h_49m_31s/checkpoints/best-epoch199-loss0.2868.pth
```

- ISIC18：train 1,886 张，val 808 张；seed=42；256×256；全部训练标注。
- 最佳权重按验证 loss 选择；同一 val 上 pooled Dice=0.8930885942，pooled IoU=0.8068293357。
- **89.31% 是历史开发参考，不能称为独立测试成绩。** 旧 val 已经被查看和用于选模型，不能通过改名变成未见测试集。
- 第 270 轮 Dice≈88.89%，第 300 轮≈53.48%。原因未查明；保存的最佳权重仍可用于诊断。
- 已完成半监督消融使用了不同 unlabeled batch size，不能直接将其差异归因于扫描机制。

### 已完成的全监督对照（2026-09-07）

全部实验 300 epochs，同一 manifest、预训练、batch=32、256×256、原 BCE+Dice，只有 P1 增加 boundary_weight=2。指标为最佳验证 pooled Dice，非独立测试。

| seed | B0 | P1 固定边界加权 | 配对差值（百分点） |
|---|---:|---:|---:|
| 42 | 88.89% | 89.55% | +0.66 |
| 43 | 89.96% | 90.09% | +0.13 |
| 44 | 89.75% | 88.86% | −0.89 |
| 均值±样本标准差 | 89.53%±0.57% | 89.50%±0.62% | −0.03 |

P1 的平均 macro Dice 也未改善；boundary F1 43.81%→44.62%，有效样本平均 HD95 15.50→15.81 像素（B0 有一个未定义样本）。**暂停固定边界加权，不继续扫权重。当前执行方案改为第 5 节的 R1 输出细化。**

旧权重诊断已经复现 pooled Dice=0.8930885942。新训练已完成真实 GPU 运行；这不代表下述新增 R1 已验证涨点。

## 2. 本次代码准备

| 文件 | 用途 |
|---|---|
| `audit_training.py` | CPU 解析历史日志，导出验证曲线及大幅下降记录；不猜测原因 |
| `prepare_full_split.py` | 按文件 ID 配对图像/掩码，冻结划分，检查路径和 train/val 完全相同的图像文件 |
| `analyze_full.py` | 加载旧/新权重，导出逐图指标、面积分组和最差案例图 |
| `train_full.py` | 独立全监督入口；逐轮验证、CSV、按 pooled Dice 选模型、原子检查点、断点恢复 |
| `fullsup/` | 数据、损失和指标实现 |

不改动原版 `train.py` 与 SSL 训练行为。新入口不是原实验的逐位复现：数据处理、随机数顺序、选模规则及默认学习率周期有明确变化，必须先跑新入口 baseline，再比较方法。

### 新入口的固定设置

- 模型配置与当前 ISIC 基线一致：depths=[2,2,2,2]，decoder=[2,2,2,1]，drop_path=0.2，单类输出。
- 默认 BCE + per-image soft Dice；全部训练样本使用人工标注；没有教师、伪标签、扫描扰动。
- AdamW，lr=0.001，weight_decay=0.01，batch_size=32，300 epochs，256×256。
- 默认 cosine T_max=epochs，避免旧 T_max=50 在 300 轮中反复下降/上升；这是一项协议变化，**尚不能据此解释旧异常**。可用 `--t-max 50` 做专门对照。
- `--preprocessing corrected`：每次增强重新采样旋转角，掩码最近邻缩放；图像仍用与旧实现代数等价的逐图 min-max 到 [0,255]，对常量图防除零。
- `--preprocessing legacy`：保留初始化单角度旋转、掩码双线性缩放；仅用于受控诊断，不保证复现旧训练随机轨迹。
- 新入口显式设图像缩放 antialias=False，与旧 torchvision 0.14 张量 resize 默认行为对齐。跨环境比较应核对 torchvision 版本。
- 默认 workers=0，保存 Python/NumPy/Torch/CUDA/DataLoader RNG；恢复必须用同一 output 与相同参数。
- 每轮记录实际训练 lr、train loss、validation loss、pooled Dice/IoU、macro Dice、耗时；检测非有限输出/梯度并失败退出。
- `best.pth` 按固定阈值 0.5 的验证 pooled Dice 保存；`latest.pth` 含优化器、调度器、RNG、历史记录。检查点通过临时文件再替换，避免覆盖到一半。
- 不在训练结束自动评估测试集。输出目录必须新建，恢复时显式 `--resume`。

## 3. 先做 CPU 核查，再花 GPU 时间

在已安装原项目依赖的环境、仓库根目录运行。以下路径为占位或已观察到的实验路径，按实际目录核对。不要在运行中的旧任务目录直接覆盖代码。

```bash
python -m pytest -q tests

python audit_training.py \
  --log results/vmunet_isic18_Friday_04_September_2026_11h_49m_31s/log/train.info.log \
  --output results/audit_full_legacy

# 首轮开发比较保留原 train/val；不覆盖已有 manifest
python prepare_full_split.py --data-path data/isic2018 \
  --output splits/full_isic18_legacy.json
```

审查项目：检查学习率与掉点时间的对应关系、恢复训练记录、数据 ID 配对、异常图像/掩码、输入范围、检查点来源。图像字节哈希只发现完全相同文件；患者/病灶泄漏、重编码或近重复图像需要元数据与额外审查。

原版 myNormalize 的固定 mean/std 随后被逐图 min-max 抵消，因此“使用源域 mean/std”本身不构成泛化机制。不要继续沿用旧文档中的相关论证。

## 4. 第一笔 GPU 预算：最佳模型错误分析

```bash
python analyze_full.py \
  --ckpt results/vmunet_isic18_Friday_04_September_2026_11h_49m_31s/checkpoints/best-epoch199-loss0.2868.pth \
  --data-path data/isic2018 \
  --manifest splits/full_isic18_legacy.json \
  --preprocessing legacy --gpu 0 --worst-k 20 \
  --output results/diagnosis_full_legacy
```

输出：

- `per_image.csv`：按 Dice 从低到高排列，含面积比例、Dice、IoU、boundary F1、HD95。
- `summary.json`：pooled 与 macro 指标分列；按 GT 面积比例分组（<5%、5–20%、≥20%）。这些是固定诊断分组，不是临床定义。
- 最差 20 张四联图：原图、GT、预测、错误图（红=误检，蓝=漏检）。
- `evaluation_pairs.json`：确切评估样本记录。

指标约定：所有指标在模型输入分辨率计算；HD95 单位是像素，不是毫米。空/空 Dice=1，单侧空 Dice=0；单侧空 HD95 未定义，输出 null/空值并单列数量，禁止只报剔除失败例后的平均 HD95。Boundary F1 容差默认 2 像素。Pooled Dice 与原项目全图像像素汇总口径一致，macro Dice 才是逐图平均，不能混用。

先检查该旧权重 pooled Dice 是否接近 0.8930886；若明显不同，停止比较，排查权重、配对、预处理和依赖。不要直接宣称新脚本已经复现该数字。

## 5. 当前实验：R1 输出端局部残差细化

假设：最后四倍空间展开后，增加局部空间融合可能改善细节恢复。现有数据只说明固定边界加权没有稳定收益，尚未证明此假设。

实现位于 `fullsup/runtime.py`，只包装 `vmunet.final_conv`：

```text
Final_PatchExpand2D → 24 通道特征 F
F + Conv1×1(GELU(Conv3×3(F))) → 原 1×1 分类层 → sigmoid
```

- `--output-refine` 开启；默认关闭即 B0，原版/SSL 模型文件不改动。
- 残差分支保持 24 通道，额外 5,808 个参数，无额外归一化或 dropout。
- 残差最后的 1×1 权重/偏置初始化为 0，初始预测等于原模型；首次梯度先更新该投影，之后传播到 3×3 层。
- 在原预训练权重加载后添加分支，保留原分类器，并恢复创建分支前的 Torch RNG，避免改变骨干初始权重和之后的随机序列。
- 使用原 BCE+Dice，boundary_weight=0；保持既有 B0 的所有训练设置。既有 B0 三种子可作对照，只有改变 batch/分辨率/其他协议时才需要重跑对应 B0。
- 检查点保存 `output_refine`；恢复参数必须一致，分析工具从检查点自动重建头部。旧 B0 配置缺少此字段时视为 False。
- 权重加载仅过滤 THOP 的 `total_ops/total_params`，其余参数严格匹配；原始 state_dict 适用于旧 B0，R1 请保留带 config 的 best/latest 检查点。

```bash
# 先通过测试，再做同 batch 的一轮 GPU 烟测（不能用于评估涨点）
python -m pytest -q tests
python train_full.py --data-path data/isic2018 \
  --manifest splits/full_isic18_legacy.json --output results/full_refine_smoke \
  --epochs 1 --batch-size 32 --output-refine --gpu 0

# R1：预先固定 seed 42 筛选；从同一预训练出发，不从 B0 best 微调
python train_full.py --data-path data/isic2018 \
  --manifest splits/full_isic18_legacy.json --output results/full_refine_s42 \
  --seed 42 --output-refine --gpu 0

# 恢复同一 R1；其余原参数保持不变
python train_full.py --data-path data/isic2018 \
  --manifest splits/full_isic18_legacy.json --output results/full_refine_s42 \
  --seed 42 --output-refine --gpu 0 --resume

# 分析时自动识别 R1，无需手工指定模型类型
python analyze_full.py --ckpt results/full_refine_s42/best.pth \
  --data-path data/isic2018 --manifest splits/full_isic18_legacy.json \
  --output results/diagnosis_full_refine_s42 --gpu 0
```

筛选时同时检查 pooled/macro Dice、boundary F1、HD95及同图失败例，不能只报最高 Dice。正向后再补 seed 43/44；单 seed 不构成方法成立证据。若 R1 三种子支持增益，再增加参数量接近的纯 1×1 头部对照，检验空间融合与额外容量的区别；该后续对照尚未实现。

R1 是实验候选，不是已经确认的新颖方法。低层/高层特征融合已有 [VM-UNetV2](https://arxiv.org/abs/2403.09157) 等工作；当前暂不增加浅层分支、多尺度模块或更高分辨率，待 R1 结果再决策。

## 6. 正式论文协议与继续/停止标准

首轮单 seed 只筛选。正向后跑 seed=42/43/44，报告均值、标准差、每个 seed 的配对差值；方法有一致收益后再补第二数据集及相关强基线。不能保证 1–2 点提升，更不保证投稿结果。

正式协议可在原 train 内冻结验证集：

```bash
python prepare_full_split.py --data-path data/isic2018 \
  --val-fraction 0.2 --seed 42 --output splits/full_isic18_internal.json
```

此时 train 内所有剩余图像都使用标注，仍是全监督；但训练样本数减少，必须重跑 baseline。旧 val 已用于开发，不应称为严格未见测试；需要新的未触碰测试集/外部数据，且按患者或病灶分组，明确历史使用情况。独立数据可以整理为 `test/images`、`test/masks`，使用 `analyze_full.py --split test`，不传 manifest。

继续：相对新 B0 的改进在多 seed 可重复，诊断目标指标同时改善，第二数据集支持；再细化方法并做文献查重。停止或改方向：收益与随机波动相当、只在单 seed 出现、边界指标改善但整体显著恶化、训练设置变化解释了收益。先分析失败，不连续堆模块。

## 7. 租赁 GPU 使用顺序

1. 保存旧任务日志和 best/latest；经用户授权再停止，不由本代码自动处理。
2. CPU 测试与日志核查；GPU 空闲时执行一次旧模型诊断。
3. 分析结果、准备代码期间，按租赁平台规则暂停计费（停止 Python 不等于停止计费）。
4. R1 同 batch 短烟测，通过后跑 seed 42；有信号才补其余种子，按顺序运行。
5. 有收益才扩大种子/数据集，不预先排队大量实验。

B0/P1 已完成并保存结果；新增 R1 本次仅准备代码，真实 CUDA 烟测与提升验证尚待运行。更新服务器时先保留并核对未提交差异，再合并 main 的兼容修复。

## 8. 验证记录

本机隔离 CPU 环境：Python 3.12、PyTorch 2.14.0、torchvision 0.29.0。22 项测试通过，包括已有扫描/SSL 划分测试，以及新的配对/去重、指标空掩码、旧评估公式对齐、边界损失梯度、错误图导出和真实训练循环的断点恢复测试（使用微型 CPU 网络）。这是 2026-09-05 的代码准备记录；此后服务器已完成 B0/P1 和旧模型复评。

R1 本机 25 项测试通过：初始预测等价、残差梯度、检查点自动重建与严格加载、基线/R1 断点恢复及旧配置兼容。另对真实 VM-UNet 的 forward_final 路径完成 CPU 检查，验证骨干权重/RNG 不变、输出形状与初始化预测等价及梯度传播。完整 CUDA/Mamba 训练与显存占用仍需服务器 GPU 烟测验证。
