# 当前 GPU 后续执行计划

更新：2026-09-13。用户已放弃 MambaLiteUNet，原seed43→42→44与revision1队列失效；不要从历史文档或旧脚本重新启动。旧扫描半监督配方也不继续扩展。

**当前唯一执行入口：[BCP少标注验证方案](BCP少标注验证方案.md)。** 它定义固定输入、源码隔离、启动命令、恢复、结果门槛与后续处理。

## 监控智能体按此顺序执行

1. 辨认运行中的任务和父脚本，确认没有重复执行者；保留旧结果与未提交源码。不要杀其他任务，也不要在运行中改源码/环境。
2. 以独立代码目录准备 `run_bcp10.sh`，复用189张监督best及原划分。输入指纹必须匹配；不重跑监督预训练。
3. 完成BCP的一轮六步CUDA检查，先复现起始Dice=0.8698412827556765（绝对误差≤1e-6），再验证学生/教师重载。失败则暂停后续训练并处理根因。
4. 检查通过后依次运行纯监督继续训练、BCP适配候选，每组1800次更新。各自从同一监督best开始，保留第0轮。此流程已有本轮任务授权，不需要逐步再次询问。
5. 运行期间读现有日志/CSV；不重复评估。中断按同一阶段配置/源码恢复，已完成阶段不重跑。脚本已有每组最佳权重的分析，不另起并行分析任务。
6. 两组完成后读取比较JSON及逐图结果。相对初始Dice至少+1个百分点且超过继续训练对照才进入下一方案准备；不自动排队其他种子/比例/候选。未达标继续CPU分析和具体方案准备，不为保持GPU忙碌而随意重训。

本阶段仅一个烟测和两个正式训练。候选或协议需要改变时先写清依据、对照及预算；不能在同一输出目录换代码或把失败尝试改名当成未运行。

在 `results/bcp10_followup_status.md` 记录实际代码指纹、阶段、进程、指标原值、资源消耗、下一步及原因。初始化不匹配、故障、阶段完成或需要决策时通知用户，正常训练无变化时不反复发送状态。

当没有满足条件的训练任务时，先确认产物已持久化，按用户已有租赁平台授权处理闲置计费；没有实例释放授权则通知用户，不擅自销毁实例/数据。停止Python不等于停止计费。

本文件供监控智能体读取执行；GitHub提交本身不会创建监控任务，也不代表其他智能体已接收。
